name: "Build and deploy"

on:
  pull_request:
  push:
    branches:
      - DO-782-ci-cd

jobs:
  
  build_and_test:
    name: "PR"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: "Lint"
        run: ./gradlew cleanAssemble